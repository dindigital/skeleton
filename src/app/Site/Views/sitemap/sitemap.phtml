<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <?php foreach ( $data['sitemap'] as $row ): ?>
    <url>
      <loc><?php echo $row->getLink() ?></loc>
      <?php if ( !is_null($row->getDate()) ): ?>
        <lastmod><?php echo $row->getDate(); ?></lastmod>
      <?php endif; ?>
    </url>
  <?php endforeach; ?>
</urlset>